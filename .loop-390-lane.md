# Lane: dig-node #390 — CAT discovery staging + lineage promotion

Branch: `loop/390-cat-staging` (from `origin/main`).
Scope: derived-hash arrivals land in `cat_admission_pending`; only lineage-proven
coins are written to `coins`. `coins` keeps `origin/main` semantics exactly.

Closes #380.
