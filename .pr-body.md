**DO NOT MERGE — DRAFT, gate round has not run. The orchestrator gates.**

Batch lane, one PR per repo (§1.4 batch-per-repo).

## Scope

1. **Adopt `chia-query` 0.20.0** — `success` narrowed to ack status 1; `TxStatus` gains
   `inclusion: MempoolInclusion` + `error: Option<String>`. Adapt `dig-wallet/src/sage/spend.rs`,
   verify the silent fix at `sage/chain.rs:503`, rewrite the three comments that the bump makes false,
   and surface the node's own error text where a caller can see it.
2. **Closes #412** — a landed spend records itself as `unresolved`, so a completed reclaim is
   re-attempted on the next pass.
3. **Closes #446** — inbound-demand backfill lands a remote-triggered capsule with no provenance
   marker (defect A). Gated behind default-OFF `DIG_NODE_INBOUND_DEMAND_CACHE`.
4. **Closes #449** — two mirror test fixtures still open the operator wallet under the L2 genesis.
5. **dig_ecosystem#382** (dig-onion privacy mode) — assessed for size; split out if materially larger
   than 1–4 combined.

## Status

Stub pushed. Implementation in progress — see the checklist comments on each ticket.

Blast radius, tests, dep bumps and the SemVer justification land in this body before the gate round.
