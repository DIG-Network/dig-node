## Task

Closes #302

Serve capsules by streaming instead of materialising a whole capsule in RAM. Capsules measure ~135 MB
on a live node, so every whole-capsule read is a real memory event on a peer-facing path.

**DRAFT -- gate round in progress. DO NOT MERGE.**

## Status

Re-enumerating the read sites at `origin/main` first. The ticket's path count was taken at a past
moment and sixteen minors have landed since, including #455 (`489c702a`) on the serve path.

Findings and the implementation follow in subsequent pushes.
