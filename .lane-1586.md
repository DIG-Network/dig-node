# Lane: #1586 read-leg locate key-trace

Investigating why dig-download reports `no providers located for ContentId::Resource{...}`
while dig-node's pool locator DID offer the connected holder and `fetch_range` was entered.

Working hypothesis (to be confirmed with runtime-printed values):
the "no providers located" message is emitted by `establish_commitment`, NOT by the locate step.
